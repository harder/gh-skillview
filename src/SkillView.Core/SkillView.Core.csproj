<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <RootNamespace>SkillView</RootNamespace>
    <AssemblyName>SkillView.Core</AssemblyName>
    <!-- NOTE: Terminal.Gui rc.7 fixed ConfigurationManager trim-safety, but we
         still keep IsAotCompatible off until the broader TG2 AOT surface is
         re-evaluated under publish verification. -->
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Terminal.Gui" Version="2.0.1" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="SkillView.Tests" />
  </ItemGroup>

</Project>
